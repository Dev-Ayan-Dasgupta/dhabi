import 'package:flutter/material.dart';

class ApplicationProgress extends StatelessWidget {
  const ApplicationProgress({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
